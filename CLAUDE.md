# Claude Code entry point

**→ Read [`AGENTS.md`](AGENTS.md).** It is the canonical agent context for this repo: project
layout, hard rules, git and release conventions, session discipline.

This file exists only because Claude Code loads `CLAUDE.md` automatically. `AGENTS.md` is the
emerging cross-tool convention, so the content lives there and every agent reads the same
instructions rather than each tool getting its own drifting copy.

**Do not add content here.** Anything you would put in this file belongs in `AGENTS.md`.
