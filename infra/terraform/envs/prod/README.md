# Prod environment — not yet built

Deliberately empty. Mirrors `../uat` once that lands (resource group
`veervrat-prod`). Per D11 in root `CLAUDE.md`, beta testers live here, not in
UAT — so this is not a "later, someday" environment, just a "after uat proves
the module shape works" one.
